name: CI

on:
  push:
    branches: ["main"]

env:
  CARGO_TERM_COLOR: always

jobs:
  build_lint_test:
    name: "build, lint, test (${{ matrix.toolchain }})"
    runs-on: "ubuntu-latest"
    strategy:
      matrix:
        toolchain:
          - stable
          - beta
          - nightly
    steps:
      - uses: actions/checkout@v6
      - uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
      - run: rustup update ${{ matrix.toolchain }} && rustup default ${{ matrix.toolchain }}
      - run: rustup component add clippy
      - run: cargo build --verbose
      - run: cargo clippy
      - run: cargo test --verbose

  doc:
    name: "doc"
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v6
      - run: cargo doc

  # check code coverage
  # - fails if coverage drops below 95%
  # - generates "tarpaulin-report.zip" artifact
  coverage:
    name: "coverage"
    runs-on: "ubuntu-latest"

    container:
      image: "xd009642/tarpaulin:develop-nightly"
      options: --security-opt seccomp=unconfined
    steps:
      - run: echo "DATESTAMP=$(date +%Y%m%d)" >> $GITHUB_ENV
      - uses: actions/checkout@v6
      - run: cargo +nightly tarpaulin -o stdout -o html --timeout 120 --fail-under 95
      - run: mv tarpaulin-report.html polycvss-coverage-${{ env.DATESTAMP }}.html
      - uses: actions/upload-artifact@v6
        with:
          name: polycvss-coverage-${{ env.DATESTAMP }}.zip
          path: polycvss-coverage-${{ env.DATESTAMP }}.html
