# disabled: replaced with "run: npx y markdownlint-cli README.md" in
# main ci.yml

inputs:
  glob:
    description: 'input file glob'
    default: '**/.md'

# runs:
#   using: "docker"
#   image: "ghcr.io/igorshubovych/markdownlint-cli:latest"
#   args:
#     - "${{ inputs.glob }}"
